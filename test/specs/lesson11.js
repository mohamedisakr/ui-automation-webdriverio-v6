describe("Lesson 11 (Enabled, isFocused, scrolIntoView methods)", () => {
  /*
  //
  xit("should detect if an element is isEnabled", () => {
    browser.url("https://webdriver.io/");
    const blog = $('[href="/blog/"]');
    const isEnabled = blog.isEnabled();
    console.log("Is blog menu item isEnabled? ", isEnabled);
  });

  //
  it("should detect if an element is isFocused", () => {
    browser.url("https://webdriver.io/");
    const searchBox = $("#search_input_react");
    const isFocused = searchBox.isFocused();
    console.log("Is blog menu item isFocused? ", isFocused);
  });

  //
  it("should move to an element", () => {
    browser.url("https://webdriver.io/");
    const githubLink = $(
      "# footer [href='https://github.com/webdriverio/webdriverio']"
    );
    browser.pause(3000);
    githubLink.scrollIntoView();
    browser.pause(3000);
  });
  */
});
