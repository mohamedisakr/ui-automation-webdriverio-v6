describe("Lesson 12 (saveScreenshot, newWindow, switchWindow methods)", () => {
  /*
  //
  xit("should save screenshot of browser view", () => {
    browser.url("https://webdriver.io/");
    const githubLink = $(
      "#footer [href='https://github.com/webdriverio/webdriverio']"
    );
    githubLink.saveScreenshot("github.png");
  });

  //
  it("should switch to another window", () => {
    browser.url("https://www.google.com/");

    browser.newWindow("https://webdriver.io/");
    browser.pause(4000);

    browser.switchWindow("https://www.google.com/");
    browser.pause(4000);

    browser.switchWindow(
      "Next-gen browser and mobile automation test framework"
    );
    browser.pause(4000);
  });
  */
});
